{
  "installCommand": "corepack enable && corepack prepare pnpm@10.19.0 --activate && pnpm i --frozen-lockfile",
  "buildCommand": "NEXT_DISABLE_ESLINT=1 pnpm -v && pnpm build",
  "framework": "nextjs"
}
